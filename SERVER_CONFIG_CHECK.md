# 🔍 服务器配置检查指南

## 📋 请在服务器上执行以下命令，然后把结果发给我

### 1. 查看当前Nginx配置

```bash
# 查看所有启用的站点
ls -la /etc/nginx/sites-enabled/

# 查看默认站点配置
cat /etc/nginx/sites-enabled/default

# 查看所有可用站点
ls -la /etc/nginx/sites-available/

# 查看Nginx主配置
cat /etc/nginx/nginx.conf | grep -A 10 "http {"
```

### 2. 查看端口占用情况

```bash
# 查看80端口占用
sudo netstat -tlnp | grep :80

# 查看所有监听端口
sudo netstat -tlnp | grep LISTEN

# 或使用ss命令
sudo ss -tlnp | grep :80
```

### 3. 查看当前运行的网站

```bash
# 查看Nginx进程
ps aux | grep nginx

# 测试当前80端口的网站
curl -I http://localhost/
```

---

## 🎯 根据您的情况，我们有以下几种方案：

### 方案1：使用不同端口（推荐）
- 漫画项目使用 **8080端口**
- 访问地址：`http://你的IP:8080/`
- 不影响现有80端口的项目

### 方案2：使用子目录
- 漫画项目放在 `/manhua` 路径下
- 访问地址：`http://你的IP/manhua/`
- 共享80端口

### 方案3：使用域名
- 如果有域名，可以配置虚拟主机
- 访问地址：`http://manhua.yourdomain.com/`
- 基于域名区分项目

---

## 📝 请提供以下信息：

1. **当前80端口部署的是什么项目？**
2. **您希望使用哪种方案？**
   - [ ] 方案1：使用8080端口（推荐）
   - [ ] 方案2：使用子目录 /manhua
   - [ ] 方案3：使用域名（如果有）
3. **执行上面命令的结果**（特别是Nginx配置）

---

## 🚀 快速检查命令（一键复制）

```bash
echo "========================================="
echo "当前Nginx配置检查"
echo "========================================="
echo ""
echo "1. 启用的站点:"
ls -la /etc/nginx/sites-enabled/
echo ""
echo "2. 80端口占用:"
sudo netstat -tlnp | grep :80
echo ""
echo "3. 默认站点配置:"
cat /etc/nginx/sites-enabled/default 2>/dev/null || echo "无默认配置"
echo ""
echo "4. 可用端口:"
echo "8080端口:" && sudo netstat -tlnp | grep :8080 || echo "  ✅ 8080端口可用"
echo "8888端口:" && sudo netstat -tlnp | grep :8888 || echo "  ✅ 8888端口可用"
echo ""
echo "========================================="
```

**请把这个命令的输出结果发给我，我会根据您的实际情况调整配置！**
