# 📖 PHP漫画管理系统 - 使用指南

## 🎯 访问地址

### 前台访问
- **URL**: http://8.149.138.212:9090/
- **默认访问码**: `1024`

### 后台管理
- **URL**: http://8.149.138.212:9090/admin88/
- **管理员账号**: `admin`
- **管理员密码**: `admin123`

---

## 🔑 第一次使用步骤

### 步骤1：登录后台管理

1. 访问后台地址：http://8.149.138.212:9090/admin88/
2. 输入管理员账号：`admin`
3. 输入管理员密码：`admin123`
4. 点击登录

### 步骤2：修改访问码（可选）

1. 登录后台后，点击左侧菜单 **"访问码管理"**
2. 输入新的访问码（默认是 `1024`）
3. 点击保存

### 步骤3：添加漫画内容

1. 点击左侧菜单 **"添加漫画"**
2. 填写漫画信息：
   - **标题**：漫画名称
   - **类型**：选择9种类型之一
   - **封面图**：上传封面图片
   - **资源链接**：填写漫画资源地址
   - **标签**：选择或创建标签
   - **状态**：连载中/已完结
3. 点击提交

### 步骤4：管理标签

1. 点击左侧菜单 **"标签管理"**
2. 可以添加、编辑、删除标签
3. 标签类型包括：
   - **date**：日期标签（如"2024-11"）
   - **letter**：字母标签（如"A-Z"）
   - **category**：分类标签（如"热血"、"恋爱"）
   - **author**：作者标签

---

## 📋 9大漫画模块说明

### 1. 日更板块 (daily_update)
- 显示每日更新的漫画
- 支持章节管理
- 按更新时间排序

### 2. 韩漫合集 (korean_collection)
- 韩国漫画合集
- 需要上传封面图
- 显示连载状态

### 3. 完结短漫 (short_complete)
- 已完结的短篇漫画
- 适合快速阅读

### 4. 日漫推荐 (japan_recommend)
- 精选日本漫画推荐
- 需要上传封面图

### 5. 日漫合集 (japan_collection)
- 日本漫画合集
- 按分类展示

### 6. 动漫合集 (anime_collection)
- 动画改编漫画
- 关联动画信息

### 7. 广播剧合集 (drama_collection)
- 有声漫画/广播剧
- 支持音频链接

### 8. 失效反馈 (feedback)
- 用户反馈失效链接
- 管理员可查看处理

### 9. 防走丢 (backup_link)
- 备用访问地址
- 网站公告信息

---

## 🎨 前台使用流程

### 访客访问流程

1. **访问首页**
   - 打开：http://8.149.138.212:9090/
   - 看到9宫格卡片界面

2. **输入访问码**
   - 点击任意模块卡片
   - 弹出访问码输入框
   - 输入访问码：`1024`
   - 点击"提交"按钮

3. **浏览漫画**
   - 访问码验证通过后
   - 可以浏览该模块的所有漫画
   - 点击漫画卡片查看详情

4. **查看详情**
   - 显示漫画封面、标题、简介
   - 显示资源链接
   - 可以点击链接跳转阅读

5. **搜索功能**
   - 使用顶部搜索框
   - 输入漫画名称或关键词
   - 查看搜索结果

---

## ⚙️ 后台管理功能

### 1. 控制台 (Dashboard)
- 显示网站统计数据
- 漫画总数、访问量等

### 2. 漫画管理
- **添加漫画**：添加新的漫画内容
- **漫画列表**：查看、编辑、删除漫画
- **批量操作**：批量删除、批量修改状态
- **分页显示**：每页20条记录

### 3. 标签管理
- **添加标签**：创建新标签
- **编辑标签**：修改标签名称和类型
- **删除标签**：删除不需要的标签
- **排序管理**：调整标签显示顺序

### 4. 访问码管理
- **修改访问码**：更新前台访问密码
- **访问码存储**：保存在数据库 `site_config` 表

### 5. 系统设置
- **网站名称**：修改网站标题
- **网站URL**：配置网站地址
- **其他配置**：时区、调试模式等

---

## 🔧 常见配置任务

### 修改访问码

**方式1：通过后台管理**
1. 登录后台
2. 点击"访问码管理"
3. 输入新密码并保存

**方式2：直接修改数据库**
```sql
-- 在MySQL中执行
UPDATE site_config SET config_value = '新密码' WHERE config_key = 'access_code';
```

### 修改管理员密码

**在服务器上执行：**
```bash
cd /root/php-manhua

# 生成新密码的哈希值
php -r "echo password_hash('新密码', PASSWORD_DEFAULT);"

# 然后在MySQL中更新
mysql -h 47.110.75.188 -u root -p123456 manhua_db -e "UPDATE admins SET password = '上面生成的哈希值' WHERE username = 'admin';"
```

### 添加新管理员

```sql
-- 在MySQL中执行
INSERT INTO admins (username, password, created_at) 
VALUES ('newadmin', '$2y$10$...哈希密码...', NOW());
```

### 清空所有漫画数据

```sql
-- 谨慎操作！会删除所有漫画
TRUNCATE TABLE mangas;
TRUNCATE TABLE manga_chapters;
```

---

## 📊 数据库表说明

### 主要数据表

1. **site_config** - 网站配置
   - `access_code`: 访问码

2. **manga_types** - 漫画类型
   - 9种预定义类型

3. **tags** - 标签表
   - date/letter/category/author 类型

4. **mangas** - 漫画主表
   - 标题、封面、状态、资源链接等

5. **manga_chapters** - 章节表
   - 用于日更板块的章节管理

6. **admins** - 管理员表
   - 默认账号：admin/admin123

7. **access_logs** - 访问日志
   - 记录用户访问记录

---

## 🚀 高级配置

### 修改网站名称

编辑配置文件：
```bash
nano /root/php-manhua/config/config.php
```

修改：
```php
'app' => [
    'name' => '你的网站名称',  // 修改这里
    'url'  => 'http://8.149.138.212:9090',
],
```

### 修改上传文件大小限制

```php
'upload' => [
    'max_size' => 10 * 1024 * 1024,  // 改为10MB
],
```

### 开启调试模式（仅开发环境）

```php
'app' => [
    'debug' => true,  // 开启调试
],
```

**注意：生产环境必须设置为 `false`！**

---

## 🔒 安全建议

### 1. 修改默认密码
- ✅ 立即修改管理员密码
- ✅ 修改访问码
- ✅ 定期更换密码

### 2. 限制后台访问
- 建议在Nginx中添加IP白名单
- 或使用VPN访问后台

### 3. 定期备份
```bash
# 备份数据库
mysqldump -h 47.110.75.188 -u root -p123456 manhua_db > backup_$(date +%Y%m%d).sql

# 备份上传的图片
tar -czf uploads_backup_$(date +%Y%m%d).tar.gz /root/php-manhua/public/uploads/
```

### 4. 监控日志
```bash
# 查看访问日志
tail -f /var/log/nginx/manhua_access.log

# 查看错误日志
tail -f /var/log/nginx/manhua_error.log
```

---

## 📱 移动端适配

项目使用 Bootstrap 5，自动适配移动端：
- ✅ 响应式布局
- ✅ 触摸友好
- ✅ 移动端优化

---

## 🐛 常见问题

### Q1: 忘记访问码怎么办？
**A**: 直接在数据库中查询：
```sql
SELECT config_value FROM site_config WHERE config_key = 'access_code';
```

### Q2: 忘记管理员密码怎么办？
**A**: 重置为默认密码 `admin123`：
```bash
cd /root/php-manhua
php -r "echo password_hash('admin123', PASSWORD_DEFAULT);"
# 将输出的哈希值更新到数据库
```

### Q3: 上传图片失败？
**A**: 检查权限：
```bash
sudo chmod -R 755 /root/php-manhua/public/uploads
sudo chown -R www-data:www-data /root/php-manhua/public/uploads
```

### Q4: 访问提示500错误？
**A**: 查看错误日志：
```bash
tail -50 /var/log/nginx/manhua_error.log
```

---

## 📞 技术支持

### 项目信息
- **项目名称**: PHP漫画管理系统
- **版本**: v1.0
- **PHP版本**: 8.0.30
- **数据库**: MySQL 5.7

### 服务器信息
- **前台地址**: http://8.149.138.212:9090/
- **后台地址**: http://8.149.138.212:9090/admin88/
- **项目路径**: /root/php-manhua
- **数据库地址**: 47.110.75.188:3306

---

## 🎉 快速开始

### 立即体验

1. **访问前台**: http://8.149.138.212:9090/
2. **输入访问码**: `1024`
3. **浏览漫画内容**

### 开始管理

1. **登录后台**: http://8.149.138.212:9090/admin88/
2. **账号**: `admin`
3. **密码**: `admin123`
4. **添加第一个漫画**

---

**祝您使用愉快！如有问题，请随时联系技术支持。** 🎊
